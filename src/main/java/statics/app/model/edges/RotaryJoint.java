package statics.app.model.edges;

import statics.app.model.nodes.Node;

public class RotaryJoint extends Node {
    //todo add pos
    /**
     * @param pId id of the new Node
     */
    public RotaryJoint(int pId) {
        super(pId);
    }
}
